# Edge Function Tests

This folder contains Deno tests for Supabase Edge Functions and shared modules.

Run with:

```bash
npm run test:edge
```

Requirements:
- Deno must be installed in the execution environment.
- CI should run this suite in the edge-function job (nightly/main).

name: Deploy Test

on:
  workflow_dispatch:
    inputs:
      git_ref:
        description: "Branch, tag, or commit SHA to deploy"
        required: true
        default: "main"

concurrency:
  group: deploy-test
  cancel-in-progress: true

jobs:
  deploy-test:
    runs-on: ubuntu-latest
    environment: test
    permissions:
      contents: read
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
      STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      STRIPE_PRICE_FLAT_MONTHLY_ID: ${{ secrets.STRIPE_PRICE_FLAT_MONTHLY_ID }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - name: Checkout selected ref
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.git_ref }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Run frontend test suite
        run: npm run test

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v2.x

      - name: Run edge function test suite
        run: npm run test:edge

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate required secrets
        shell: bash
        run: |
          set -euo pipefail
          missing=0
          for secret_name in SUPABASE_ACCESS_TOKEN SUPABASE_PROJECT_REF SUPABASE_DB_PASSWORD STRIPE_SECRET_KEY STRIPE_WEBHOOK_SECRET STRIPE_PRICE_FLAT_MONTHLY_ID VERCEL_TOKEN VERCEL_ORG_ID VERCEL_PROJECT_ID; do
            if [[ -z "${!secret_name:-}" ]]; then
              echo "::error::Missing required environment secret: ${secret_name}"
              missing=1
            fi
          done
          if [[ "$missing" -ne 0 ]]; then
            exit 1
          fi

      - name: Show deploy target (test)
        run: |
          echo "GitHub environment: test"
          echo "Supabase project ref: $SUPABASE_PROJECT_REF"

      - name: Link Supabase project (test)
        run: supabase link --project-ref "$SUPABASE_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"

      - name: Sync Supabase Edge secrets (test)
        shell: bash
        run: |
          set -euo pipefail
          supabase secrets set \
            --project-ref "$SUPABASE_PROJECT_REF" \
            STRIPE_SECRET_KEY="$STRIPE_SECRET_KEY" \
            STRIPE_WEBHOOK_SECRET="$STRIPE_WEBHOOK_SECRET" \
            STRIPE_PRICE_FLAT_MONTHLY_ID="$STRIPE_PRICE_FLAT_MONTHLY_ID"

      - name: Push database migrations (test)
        run: supabase db push --linked --yes

      - name: Deploy edge functions (test)
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t functions_to_deploy < <(
            find supabase/functions -mindepth 1 -maxdepth 1 -type d ! -name "_shared" \
              | while read -r dir; do
                  if [[ -f "$dir/index.ts" ]]; then
                    basename "$dir"
                  fi
                done \
              | sort -u
          )

          for fn in "${functions_to_deploy[@]}"; do
            echo "Deploying edge function: ${fn}"
            supabase functions deploy "$fn" --project-ref "$SUPABASE_PROJECT_REF" --no-verify-jwt
          done

      - name: Pull Vercel project settings (preview)
        run: npx vercel pull --yes --environment=preview --token "$VERCEL_TOKEN"

      - name: Build Vercel preview artifact
        run: npx vercel build --token "$VERCEL_TOKEN"

      - name: Deploy Vercel preview
        run: npx vercel deploy --prebuilt --token "$VERCEL_TOKEN"
